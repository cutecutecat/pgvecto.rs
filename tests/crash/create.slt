statement ok
CREATE TABLE t (val vector(3));

statement ok
INSERT INTO t (val) SELECT ARRAY[i, i, i]::real[] FROM generate_series(1, 1000) AS i;

statement ok
CREATE INDEX i ON t USING vectors (val vector_l2_ops)
WITH (options = "[indexing.hnsw]");

query I
SELECT * FROM t ORDER BY val <-> '[1.7,1.7,1.7]' limit 2;
----
[2, 2, 2]
[1, 1, 1]